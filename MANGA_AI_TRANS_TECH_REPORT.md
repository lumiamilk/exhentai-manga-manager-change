# 漫画 AI 识别与无损嵌字技术调研报告 (2024-2025)

## 1. 核心痛点分析
在目前的漫画翻译流程中，主要存在两大技术瓶颈：
1.  **识别难 (OCR Transition)**：传统的 OCR (如 PaddleOCR, EasyOCR) 依赖字符分割和线性排列，对漫画中**纵排文字**、**跨行标注**以及**潦草手写体（拟声词、边注）**识别率极低。
2.  **嵌字脏 (Inpainting Artifacts)**：简单的图像修复算法在抹除原日文时，会破坏漫画特有的**网点 (Screentones)** 和背景线条，产生类似“白色涂改液”或“脏色块”的伪影。

---

## 2. 最前沿 OCR 技术方案 (解决识别难)

目前 Hugging Face 和开源社区的 SOTA (State-of-the-Art) 模型已转向 **Vision Transformer (ViT)** 架构。

### A. Manga-OCR (推荐：行业金标准)
*   **模型**: `kha-chan/manga-ocr-base` (Hugging Face)
*   **原理**: 采用端到端的 ViT + Transformer Decoder 架构。它不进行传统的“切字”，而是将整块文字区域视为视觉特征序列。
*   **优势**: 专门针对漫画训练，对**手写体**、**纵排文字**、**艺术字**有极强的鲁棒性。它是目前唯一能准确识别“潦草日语”的开源本地模型。

### B. 多模态大模型 (VLM)
*   **模型**: GPT-4o / Claude 3.5 Sonnet / InternVL2
*   **优势**: 具备极强的上下文推理能力，能够根据画面剧情推断模糊的文字内容。
*   **局限**: 成本高、API 延迟大，不适合大规模本地全自动处理。

---

## 3. 无损背景修复技术 (解决嵌字脏)

解决“涂改液”感的关键在于 **纹理感知修复 (Texture-aware Inpainting)**。

### A. LaMa (Large Mask Inpainting)
*   **模型**: `advimman/lama`
*   **原理**: 使用快速傅里叶卷积 (FFCs)，具备全局感受野。
*   **优势**: 在处理大面积文字抹除时，能完美还原背景的**重复性网点 (Screentones)**，不会产生模糊感。它是目前解决“弄脏图片”的最佳本地化方案。

### B. 精准分割 (TextSeg)
*   **技术**: 基于 Segment Anything (SAM) 或定制的文字分割模型。
*   **原理**: 不再使用矩形框覆盖，而是生成像素级的文字 Mask。
*   **优势**: 只修改文字像素，100% 保留文字边缘以外的背景细节，从根源上减少“涂改感”。

---

## 4. 推荐的端到端集成路径

为了在本项目（或类似管理器）中实现极致体验，建议采用以下流水线：

1.  **检测层**: 使用定制的 SegNet 获取像素级文字 Mask。
2.  **识别层**: 调用 **Manga-OCR** 处理选定区域，解决手写日语识别。
3.  **修复层**: 使用 **LaMa** 引擎根据 Mask 进行背景补全。
4.  **渲染层 (非破坏性)**: 
    *   **建议**: 不要直接修改原图。
    *   **实现**: 在阅读器 (如 Vue 组件) 中生成一个与原图等大的透明 **SVG/Canvas 层**。将翻译后的文本渲染在该层，支持自定义字体（漫画体）、描边和外发光。这样原图保持无损，且翻译文本可随时调整。

---

## 5. 开发者参考项目
*   **Manga-Image-Translator**: 目前开源界集成度最高的项目，集成了上述大部分技术。
*   **IOPaint (Lama-Cleaner)**: 处理背景修复的最强工具。
*   **Manga-Copy**: 优秀的本地 OCR 辅助工具。

---
*本报告由 Gemini CLI 调研生成，旨在为 AI 代理和开发者提供技术演进方向。*
